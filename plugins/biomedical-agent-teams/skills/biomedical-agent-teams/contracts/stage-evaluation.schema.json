{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Biomedical Agent Teams Stage Evaluation",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "evaluation_id", "plugin_version", "workflow_run_id", "created_at", "workflow_alias", "stages", "overall_verdict"],
  "properties": {
    "schema_version": { "const": "2.0" },
    "evaluation_id": { "type": "string" },
    "plugin_version": { "type": "string", "minLength": 1 },
    "workflow_run_id": { "type": "string", "minLength": 1 },
    "created_at": { "type": "string", "format": "date-time" },
    "workflow_alias": { "type": "string" },
    "stages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["stage_id", "stage_name", "status", "evidence", "blocking_issues"],
        "properties": {
          "stage_id": {
            "type": "string",
            "enum": ["S1", "S2", "S3", "S4", "S5", "other"]
          },
          "stage_name": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["pass", "pass-with-caveats", "skipped", "block", "not-applicable"]
          },
          "score": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          },
          "evidence": { "type": "string" },
          "blocking_issues": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    },
    "overall_verdict": {
      "type": "string",
      "enum": ["pass", "pass-with-caveats", "block", "not-assessable"]
    },
    "downgrade_rule_applied": { "type": "string" }
  }
}
